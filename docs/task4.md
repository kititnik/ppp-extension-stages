# Отчёт по заданию 4

## Ход разработки:
1. Добавлена структура `OSRam` с полем `ram_mb` для представления оперативной памяти.
2. Расширено обобщение `OperationSystem` для включения `OSRam`.
3. Добавлены специализации `OSRam` для `LinuxOS` и `WindowsOS`.
4. Расширено обобщение `InputTag` для поддержки ввода данных `OSRam` для `LinuxOS` (`ram_linuxOS`) и `WindowsOS` (`ram_windowsOS`).
5. Реализованы функции ввода/вывода для `OSRam`, использующие полиморфный ввод через `InputTag`.
