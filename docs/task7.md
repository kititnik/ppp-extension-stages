# Отчёт по заданию 7

## Ход разработки:
1. Добавлены обобщение `MacOS` и его реализация в `mac_os.c`.
2. Добавлены заголовок `mac_in_out.h` и его реализация в `mac_in_out.c` для полиморфного ввода/вывода объектов `MacOS`.
3. Расширены обобщённые функции `combination_info` в `mac_os.c` для обработки комбинаций типов `MacOS` с `LinuxOS` и `WindowsOS`.

