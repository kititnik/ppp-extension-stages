# Отчёт по заданию 5

## Ход разработки:
1. Реализована функция `container_linux_only_out` в отдельном файле для вывода только объектов `LinuxOS`.
2. Заменил вызов функции для вывода всего контейнера на вызов `container_linux_only_out` в `main.c`.
