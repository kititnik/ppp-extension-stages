# Отчёт по заданию 3

## Ход разработки:
1.  Добавлен заголовок `os_inv_apps.h` с функцией `calculate_inv_apps` для вычисления обратного значения от количества `installed_apps` для `OperationSystem`.
2.  Добавлена реализация `calculate_inv_apps` в `os_inv_apps.c`.
3.  В `main.c` добавлена функция `write_inv_apps_to_file`, которая итерируется по вектору `OperationSystem`, вычисляет `inv_apps` для каждой ОС и записывает результат в файл.

В результате работы над заданием 3 был изменён только клиентсткий `main.c`